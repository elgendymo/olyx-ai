# Copy to .env (git-ignored) and fill in. .gitignore is the secret boundary.
# LLM provider auto-detects: local => Ollama; set HF_TOKEN (e.g. on Streamlit Cloud) => Hugging Face.
HF_TOKEN=
ANTHROPIC_API_KEY=
FEED_BASE_URL=https://renewable-price-feed-mock.onrender.com
